language: python
python:
  - "3.6"
branches:
  only:
  - develop
install:
  - pip install coverage
  - pip install -r requirements.txt
script:
  - coverage run -m pytest
  - coverage report
  - coveralls
